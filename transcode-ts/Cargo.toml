[package]
name = "transcode-ts"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "MPEG Transport Stream container support for transcode"
keywords = ["video", "container", "mpeg-ts", "transport-stream", "broadcast"]
categories = ["multimedia::video", "parser-implementations"]

[dependencies]
transcode-core = { path = "../transcode-core" }

thiserror.workspace = true
tracing.workspace = true

[dev-dependencies]
pretty_assertions.workspace = true
tempfile.workspace = true

[features]
default = []

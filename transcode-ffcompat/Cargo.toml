[package]
name = "transcode-ffcompat"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "FFmpeg-compatible CLI argument translation for drop-in replacement workflows"
keywords = ["ffmpeg", "compatibility", "migration", "cli", "transcoding"]
categories = ["multimedia"]

[dependencies]
transcode-core = { path = "../transcode-core" }
transcode-compat = { path = "../transcode-compat" }

# Error handling
thiserror.workspace = true

# Serialization
serde = { workspace = true }
serde_json.workspace = true

# Logging
tracing.workspace = true

[dev-dependencies]
pretty_assertions.workspace = true
